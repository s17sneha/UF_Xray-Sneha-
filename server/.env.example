# MongoDB Atlas connection string
# Replace <db_password> with your actual password
MONGODB_URI=mongodb+srv://vishal:<db_password>@cluster0.srf1tfo.mongodb.net/ufxray?retryWrites=true&w=majority

# JWT secret for token signing (use a long random string)
JWT_SECRET=your-super-secret-jwt-key-here

# Server port (optional, defaults to 5000)
PORT=5000

# OpenAI API key for chatbot (required for /api/chat)
OPENAI_API_KEY=your-openai-api-key-here
# Optional: override model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: rate limit for /api/chat (requests per minute, default 30)
CHAT_RATE_LIMIT=30

# ---------------- SMTP (for /api/contact) ----------------
# Configure these to enable email delivery from the Contact form.
# Ask your mail provider for the correct values. Examples:
#  - Gmail (with App Passwords):
#      SMTP_HOST=smtp.gmail.com
#      SMTP_PORT=587
#  - Outlook/Microsoft 365:
#      SMTP_HOST=smtp.office365.com
#      SMTP_PORT=587
#  - SSL/465 servers:
#      SMTP_PORT=465
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional sender (defaults to SMTP_USER if omitted)
MAIL_FROM=

# Default recipient for contact messages
CONTACT_TO=vishalhgiri@gmail.com
